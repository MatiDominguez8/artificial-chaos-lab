[project]
name = "artificial-chaos-lab"
version = "0.1.0"
description = "Playful multi-agent simulation experiments"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
